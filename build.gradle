apply plugin: 'java-base'
apply plugin: 'build-dashboard'

allprojects {
    group = 'com.enonic.xp.ui_testing'
    version = '1.0'

    repositories {
        mavenLocal()

        maven {
            url 'https://repo.enonic.com/dev'
        }
    }
}
