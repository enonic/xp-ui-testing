apply plugin: 'java-base'
apply plugin: 'build-dashboard'



allprojects {
    group = 'com.enonic.xp'

    repositories {
        mavenLocal()
        jcenter()
        maven {
            url 'https://repo.enonic.com/public'
        }
    }
}
