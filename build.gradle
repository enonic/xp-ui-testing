apply plugin: 'java-base'
apply plugin: 'build-dashboard'

allprojects {
    group = 'com.enonic.xp.ui_testing'
    version = '1.0'

    repositories {
        mavenLocal()
        mavenCentral()
        maven {
            url 'http://repo.enonic.com/dev'
        }
    }
}
